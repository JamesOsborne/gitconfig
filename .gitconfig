[user]
	name = JamesOsborne
	email = james.osborne112@gmail.com
[alias]
    co = checkout
    com = commit -m
    st = status
    br = branch
    hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
    type = cat-file -t
    dump = cat-file -p
    pushup = push --set-upstream
